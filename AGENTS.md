# Agent Notes

## Skill Development

This repo holds agent skills under `skills/`. After creating or editing any
skill, validate its `SKILL.md` before committing:

```bash
tessl skill review <SKILL.md>
```

Run it against each changed skill (e.g. `tessl skill review skills/productivity/setup-repo/SKILL.md`)
and resolve the reported issues before publishing.
